/**
 * Fair-use caps, shared between server (entitlement.ts) and client
 * (paywall-modal.tsx, dashboard copy) — no "server-only" here on purpose,
 * these are just numbers, not secrets.
 */
export const SUBJECT_REGEN_CAP = 4;
export const FAIR_USE_SUBJECT_CAP = 6;
